# Core
anthropic
pydantic>=2
python-dotenv
langgraph
langgraph-checkpoint-sqlite   # durable human-in-the-loop checkpoints

# Policy RAG - local embeddings backend (default)
sentence-transformers
numpy

# Policy RAG - API embeddings backend (only if EMBEDDINGS=voyage)
# voyageai

# Dashboard
streamlit
# pandas
